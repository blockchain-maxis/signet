// Package spec will hold the request/response types for a Signet
// deployment's HTTP API, so the rest of the CLI talks to it through typed
// Go values instead of hand-built JSON. Scaffolded here; implemented in a
// follow-up issue.
package spec
