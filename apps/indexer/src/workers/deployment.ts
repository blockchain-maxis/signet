// TODO(signet): scan linked wallets' operation history for contract
// deployments (create_contract / upload_wasm) and record them.

export async function runDeploymentWorker(): Promise<void> {
  // TODO(signet): implement.
}
