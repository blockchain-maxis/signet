// TODO(signet): poll the identity-registry contract for new wallet -> handle
// attestations and persist verified bindings.

export async function runAttestationWorker(): Promise<void> {
  // TODO(signet): implement.
}
