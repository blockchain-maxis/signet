// TODO(signet): aggregate per-contract activity (invocations, unique callers)
// into periodic snapshots for the public profile.

export async function runActivityWorker(): Promise<void> {
  // TODO(signet): implement.
}
