// Dashboard layout (app.signet.dev).
// TODO(signet): auth guard, app shell / nav, wallet-connect entry point.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <main>{children}</main>;
}
