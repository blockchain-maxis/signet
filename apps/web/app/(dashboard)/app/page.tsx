// Placeholder dashboard home (app.signet.dev).
export default function DashboardPage() {
  return (
    <div>
      <h1>Signet — dashboard</h1>
      <p>route: (dashboard) /app</p>
    </div>
  );
}
