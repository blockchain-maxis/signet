// Marketing layout (signet.dev root).
// TODO(signet): real marketing chrome (nav, footer, theming).
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <main>{children}</main>;
}
